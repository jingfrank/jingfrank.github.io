---
import BlogPost from '../../layouts/BlogPost.astro';

const title = '我的 Neovim 配置之路：从零到 LazyVim';
const date = '2026-07-06';
---

<BlogPost title={title} date={date}>
  最近把我的编辑器从 VS Code 换到了 Neovim，折腾了一通，记录一下过程。

  ## 为什么换编辑器

  之前一直用 VS Code，各方面都挺好的。但最近看到越来越多人在用 Neovim，几个点打动了我：

  - **快**：启动几乎是瞬间的事
  - **全键盘操作**：手不用离开键盘，思路不会打断
  - **配置文件即代码**：配置本身就是 git 仓库，换机器直接拉下来就行

  ## 用 agent 一键配置

  我对 Neovim 的配置生态不太熟悉，所以直接让 AI agent 帮我搞定了。它做了这些事情：

  1. 安装了 Neovim 0.11.4
  2. 克隆了 LazyVim 框架
  3. 配置了 40 个插件，覆盖了日常开发需要的方方面面

  最终效果让我挺满意的——**开箱即用，不用自己折腾**。

  ## 我现在的环境

  ### 主题

  用了 **Catppuccin** 暗色主题，配色很舒服，长时间看代码不累。

  ### 日常高频功能

  - **Telescope**：按 `Ctrl+P` 搜文件，按 `<leader>fg` 搜文字内容，基本取代了文件管理器
  - **Treesitter**：代码语法高亮，比正则表达式那种准确得多
  - **mini.files**：内置文件浏览，按 `<leader>e` 就能调出来
  - **LSP**：自动补全、跳转定义、错误提示，体验和 IDE 差不多
  - **blink.cmp**：代码补全引擎，比默认的好用

  ### Git 增强

  **gitsigns** 会在行号旁边显示 git 变更标记（新增/修改/删除），一眼能看出改了哪些行。后面还打算装 **lazygit**，在 Neovim 里直接管理 git。

  ### 状态栏

  用了 **lualine**，底部显示当前模式、文件名、文件类型、光标位置等信息，简洁实用。

  ### 中文友好

  配置文件里加了 GBK/GB2312 编码支持，打开中文项目不会乱码，这个对国内开发者挺实用的。

  ### 自定义快捷键

  不是完全用默认键位，我改了几个高频操作：

  | 快捷键 | 作用 |
  |--------|------|
  | `Alt+J` / `Alt+K` | 上下移动当前行 |
  | `<leader>w` | 保存文件 |
  | `<leader>q` | 关闭窗口 |
  | `Ctrl+H/J/K/L` | 在窗口间跳转 |
  | `Esc` | 清除搜索高亮 |

  ## 感受

  刚开始学习曲线确实有点陡，vim 的模式切换需要适应几天。但习惯之后确实回不去了——**编辑器的存在感变低了**，更多注意力放在代码本身。

  另外配置文件本身就是 git 仓库这件事真的很爽，换机器只需要：

  ```bash
  git clone git@github.com:jingfrank/nvim-config.git ~/.config/nvim
  nvim
  ```

  装好 lazy.nvim 后它会自动拉取所有插件，几分钟就能恢复完整的开发环境。

  ## 接下来还想折腾的

  - 装上 lazygit，更高效地管理 git
  - 配置 Copilot，尝试 AI 辅助编程
  - 折腾一下 markdown 预览插件，方便写博客

  等折腾完了再来更新 😄
</BlogPost>